version: '3.7'
services:
  gusion:
    environment:
      - PYTHONUNBUFFERED=1
    build:
      context: .
      dockerfile: Dockerfile
    command: tail -f /dev/null
    container_name: "gusion"
    image: "gusion:latest"
    network_mode: "bridge"
    # volumes:
    #   - "$PWD:/app"